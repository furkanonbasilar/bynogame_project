import * as TYPES from "./actionTypes";

const initialState = {
  cards: [],

};

export default (state = initialState, action) => {
  switch (action.type) {
    case TYPES.: {
      return Object.assign({}, state, {
   
      });
    }
    case TYPES.: {
      return Object.assign({}, state, {

      });
    }
    default:
      return state;
  }
};